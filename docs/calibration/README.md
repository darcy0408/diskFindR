# Calibration Marker

This directory contains a printable placeholder calibration board for the MVP. The next advanced phase should replace it with a generated OpenCV ArUco marker sheet and tests that confirm marker detection with JavaCV/OpenCV.

Print at 100 percent scale and place flat near the tee. In Solo Mode, missing calibration does not block analysis; it widens uncertainty.
